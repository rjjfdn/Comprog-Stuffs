CREATE TABLE ACACT                                                                                        
(
ACA_ID INT NOT NULL,
ACH_ID INT NOT NULL,
ACA_ITEM VARCHAR(60),
ACA_TARGET DATE,
ACA_COMPL DATE,
ACA_REMIND DATE,
ACA_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACA_ID)
);

CREATE TABLE ACCAT                                                                                        
(
ACC_ID INT NOT NULL,
ACC_CODE VARCHAR(6),
ACC_NAME VARCHAR(20),
ROWVER INT DEFAULT 0,
ACC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACC_ID)
);

CREATE TABLE ACCSTATUS                                                                                    
(
ACS_ID INT NOT NULL,
ACS_CODE VARCHAR(6),
ACS_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
ACS_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACS_ID)
);

CREATE TABLE ACCTYPE                                                                                      
(
ACT_ID INT NOT NULL,
ACT_CODE VARCHAR(6),
ACT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
ACT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACT_ID)
);

CREATE TABLE ACEST                                                                                        
(
ACE_ID INT NOT NULL,
ACH_ID INT NOT NULL,
ACE_ITEM VARCHAR(60),
ACE_DESC VARCHAR(60),
ACE_NAME VARCHAR(32),
ACE_ADD1 VARCHAR(30),
ACE_ADD2 VARCHAR(30),
ACE_ADD3 VARCHAR(30),
ACE_ADD4 VARCHAR(30),
ACE_PCODE VARCHAR(9),
ACE_TEL VARCHAR(18),
ACE_VALUE DECIMAL,
ACE_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACE_ID)
);

CREATE TABLE ACHDR                                                                                        
(
ACH_ID INT NOT NULL,
ACT_ID INT NOT NULL,
ACC_ID INT NOT NULL,
ACH_SYSNO VARCHAR(8) NOT NULL,
ACH_SUMMARY VARCHAR(55),
ACH_STFID INT,
ACH_DTREP DATE,
ACH_TMREP VARCHAR(5),
ACH_DTOCCUR DATE,
ACH_TMOCCUR VARCHAR(5),
ACH_LOCATION VARCHAR(55),
ACH_FIRE TINYINT(1) DEFAULT 0,
ACH_POLICE TINYINT(1) DEFAULT 0,
ACH_AMBULAN TINYINT(1) DEFAULT 0,
ACH_STFID2 INT,
ACH_DTREVIEW DATE,
ACH_NOTES TEXT,
ACH_STATUS TINYINT(1),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACH_ID)
);

CREATE TABLE ACIMG                                                                                        
(
ACI_ID INT NOT NULL,
ACH_ID INT NOT NULL,
ACI_DESC VARCHAR(60),
ACI_ITEM VARCHAR(40),
ACI_IMG TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACI_ID)
);

CREATE TABLE ACLOSS                                                                                       
(
ACL_ID INT NOT NULL,
ACH_ID INT NOT NULL,
ACL_ITEM VARCHAR(40),
ACL_VALUE DECIMAL,
ACL_NOTES TEXT,
ACL_CLAIM TINYINT(1) DEFAULT 0,
ACL_EST TINYINT(1) DEFAULT 0,
ACL_SETT TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACL_ID)
);

CREATE TABLE ACPERS                                                                                       
(
ACP_ID INT NOT NULL,
ACH_ID INT NOT NULL,
STF_ID INT,
ACP_FNAME VARCHAR(20),
ACP_LNAME VARCHAR(20),
ACP_ADD1 VARCHAR(30),
ACP_ADD2 VARCHAR(30),
ACP_ADD3 VARCHAR(30),
ACP_ADD4 VARCHAR(30),
ACP_PCODE VARCHAR(9),
ACP_TEL VARCHAR(18),
ACP_MOBILE VARCHAR(18),
ACP_REGNO VARCHAR(7),
ACP_DRIVER TINYINT(1) DEFAULT 0,
ACP_MED TINYINT(1) DEFAULT 0,
ACP_CLAIM TINYINT(1) DEFAULT 0,
ACP_SETT TINYINT(1) DEFAULT 0,
ACP_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACP_ID)
);

CREATE TABLE ACSETT                                                                                       
(
ACS_ID INT NOT NULL,
ACH_ID INT NOT NULL,
ACS_ITEM VARCHAR(60),
ACS_DESC VARCHAR(60),
ACS_INSCO VARCHAR(32),
ACS_ADD1 VARCHAR(30),
ACS_ADD2 VARCHAR(30),
ACS_ADD3 VARCHAR(30),
ACS_ADD4 VARCHAR(30),
ACS_PCODE VARCHAR(9),
ACS_TEL VARCHAR(18),
ACS_VALUE DECIMAL,
ACS_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACS_ID)
);

CREATE TABLE ACTYPE                                                                                       
(
ACT_ID INT NOT NULL,
ACT_CODE VARCHAR(6),
ACT_NAME VARCHAR(20),
ROWVER INT DEFAULT 0,
ACT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACT_ID)
);

CREATE TABLE ACVEH                                                                                        
(
ACV_ID INT NOT NULL,
ACH_ID INT NOT NULL,
FLT_ID INT,
ACV_REGNO VARCHAR(7),
ACV_MNF VARCHAR(20),
ACV_MDL VARCHAR(25),
ACV_YEAR VARCHAR(4),
ACV_DETAIL TEXT,
ACV_DMG TEXT,
ACV_SETT TINYINT(1) DEFAULT 0,
ACV_CLAIM TINYINT(1) DEFAULT 0,
ACV_EST TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACV_ID)
);

CREATE TABLE ACWIT                                                                                        
(
ACW_ID INT NOT NULL,
ACH_ID INT NOT NULL,
STF_ID INT,
ACW_FNAME VARCHAR(20),
ACW_LNAME VARCHAR(20),
ACW_ADD1 VARCHAR(30),
ACW_ADD2 VARCHAR(30),
ACW_ADD3 VARCHAR(30),
ACW_ADD4 VARCHAR(30),
ACW_PCODE VARCHAR(9),
ACW_TEL VARCHAR(18),
ACW_MOBILE VARCHAR(18),
ACW_STATE TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ACW_ID)
);

CREATE TABLE BUDGET                                                                                       
(
BUD_ID INT NOT NULL,
BUD_CODE VARCHAR(6),
BUD_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
BUD_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (BUD_ID)
);

CREATE TABLE CHECKSCT                                                                                     
(
CKS_ID INT NOT NULL,
CKS_NAME VARCHAR(80),
ROWVER INT DEFAULT 0,
CKS_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CKS_ID)
);

CREATE TABLE CHGRENEW                                                                                     
(
CGR_ID INT NOT NULL,
FCG_ID INT NOT NULL,
SUP_ID INT NOT NULL,
NOM_ID INT NOT NULL,
ROWVER INT DEFAULT 0,
CGR_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
CGR_DATE DATE,
CGR_FEE DECIMAL DEFAULT 0,
CGR_DESC VARCHAR(60),
CGR_NOTES TEXT,
CGR_TIMEBASE TINYINT(1),
CGR_TIMEPER INT,
CGR_EXPDATE DATE,
CGR_SYSNO VARCHAR(7),
CGR_DTPREVEXP DATE,
FLT_ID INT,
CGR_STATUS TINYINT(1),
CGR_DTCOMM DATE,
PRIMARY KEY (CGR_ID)
);

CREATE TABLE CHGTYPE                                                                                      
(
CHG_ID INT NOT NULL,
CHG_CODE VARCHAR(6),
CHG_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
CHG_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
CHG_NOTES TEXT,
PRIMARY KEY (CHG_ID)
);

CREATE TABLE CHKLCAT                                                                                      
(
CKC_ID INT NOT NULL,
CKC_CODE VARCHAR(6),
CKC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
CKC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CKC_ID)
);

CREATE TABLE CHKLINK                                                                                      
(
CKK_ID INT NOT NULL,
CKL_ID INT NOT NULL,
CKI_ID INT NOT NULL,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CKK_ID)
);

CREATE TABLE CHKLIST                                                                                      
(
CKL_ID INT NOT NULL,
CKL_CODE VARCHAR(6),
CKL_NAME VARCHAR(60),
ROWVER INT DEFAULT 0,
CKL_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CKL_ID)
);

CREATE TABLE CHKLITEM                                                                                     
(
CKI_ID INT NOT NULL,
CKC_ID INT NOT NULL,
CKI_CODE VARCHAR(6),
CKI_NAME VARCHAR(60),
ROWVER INT DEFAULT 0,
CKI_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CKI_ID)
);

CREATE TABLE CONTRACT                                                                                     
(
CON_ID INT NOT NULL,
CON_CODE VARCHAR(6),
ROWVER INT DEFAULT 0,
CON_INACTIVE TINYINT(1) DEFAULT 0,
CON_NAME VARCHAR(32),
CUS_ID INT NOT NULL,
CON_DTSTART DATETIME(6),
CON_DTEND DATETIME(6),
CON_DTCANCEL DATETIME(6),
CON_RATE DECIMAL DEFAULT 0,
CON_RATEBASE TINYINT(1),
CON_LENGTH DECIMAL DEFAULT 0,
CON_LENBASE TINYINT(1),
CON_NOTES TEXT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (CON_ID)
);

CREATE TABLE COST                                                                                         
(
COS_ID INT NOT NULL,
COS_CODE VARCHAR(6),
COS_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
COS_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (COS_ID)
);

CREATE TABLE CUSTOMER                                                                                     
(
CUS_ID INT NOT NULL,
CUS_CODE VARCHAR(8),
CUS_NAME VARCHAR(32),
CUS_ADD1 VARCHAR(30),
CUS_ADD2 VARCHAR(30),
CUS_ADD3 VARCHAR(30),
CUS_ADD4 VARCHAR(30),
CUS_PCODE VARCHAR(9),
CUS_TEL VARCHAR(18),
CUS_FAX VARCHAR(18),
CUS_EMAIL VARCHAR(80),
CUS_CONTACT VARCHAR(32),
CUS_NOTES TEXT,
CUS_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CUS_CONTACT2 VARCHAR(32),
CUS_EMAIL2 VARCHAR(80),
CUS_MOBILE VARCHAR(18),
CUS_LABRATE DECIMAL DEFAULT 0,
CUS_DTRATESET DATETIME(6),
CUS_UPLPRICE TINYINT(1),
CUS_UPLMETHOD TINYINT(1),
CUS_UPLPCENT DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
INVCHARGETYPE TINYINT(1),
INVCHARGEAMT DECIMAL DEFAULT 0,
INVCHARGETEXT VARCHAR(120),
PRIMARY KEY (CUS_ID)
);

CREATE TABLE DBINFO                                                                                       
(
DBINF_NAME VARCHAR(45) NOT NULL,
DBINF_TYPE CHAR(1),
DBINF_VALUE VARCHAR(45),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DBINF_NAME)
);

CREATE TABLE DEFCAT                                                                                       
(
DCT_ID INT NOT NULL,
DCT_CODE VARCHAR(6),
DCT_NAME VARCHAR(65),
ROWVER INT DEFAULT 0,
DCT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DCT_ID)
);

CREATE TABLE DEFECT                                                                                       
(
DFT_ID INT NOT NULL,
FLT_ID INT NOT NULL,
DCT_ID INT NOT NULL,
RPC_ID INT,
WKO_ID INT,
DFT_STATUS TINYINT(1),
DFT_SEQCODE VARCHAR(10),
DFT_DATEREP DATE,
DFT_DATECLOSE DATE,
DFT_REPBY VARCHAR(32),
ROWVER INT DEFAULT 0,
DFT_INACTIVE TINYINT(1) DEFAULT 0,
DFT_NOTES TEXT,
DFT_DESC VARCHAR(65),
DFT_LEVEL TINYINT(1),
WKP_ID INT,
DFI_ID INT,
CRSYNC_ID INT NOT NULL,
STF_ID INT,
DFT_REFNO VARCHAR(20),
PRIMARY KEY (DFT_ID)
);

CREATE TABLE DEFECT_CATLIST                                                                               
(
DCL_ID INT NOT NULL,
PARENT_ID INT,
DESCRIPTION VARCHAR(100),
SEVERITY INT,
ROWVER INT DEFAULT 0,
DCL_INACTIVE TINYINT(1) DEFAULT 0,
PRIMARY KEY (DCL_ID)
);

CREATE TABLE DEFECT_CHECKDTL                                                                              
(
DCKD_ID INT NOT NULL,
DCKH_ID INT NOT NULL,
DCL_ID INT NOT NULL,
ROWVER INT DEFAULT 0,
PRIMARY KEY (DCKD_ID)
);

CREATE TABLE DEFECT_CHECKHDR                                                                              
(
DCKH_ID INT NOT NULL,
STF_ID INT NOT NULL,
FLT_ID INT NOT NULL,
DCKH_DTENTRY DATETIME(6),
DCKH_NIL TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
PRIMARY KEY (DCKH_ID)
);

CREATE TABLE DEFITEM                                                                                      
(
DFI_ID INT NOT NULL,
RPC_ID INT NOT NULL,
DFI_DESC VARCHAR(65),
ROWVER INT DEFAULT 0,
DCT_ID INT NOT NULL,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DFI_ID)
);

CREATE TABLE DEPT                                                                                         
(
DPT_ID INT NOT NULL,
DPT_CODE VARCHAR(6),
DPT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DPT_INACTIVE TINYINT(1) DEFAULT 0,
DPT_ADD1 VARCHAR(30),
DPT_ADD2 VARCHAR(30),
DPT_ADD3 VARCHAR(30),
DPT_ADD4 VARCHAR(30),
DPT_PCODE VARCHAR(9),
DPT_TEL VARCHAR(18),
DPT_FAX VARCHAR(18),
DPT_EMAIL VARCHAR(80),
DPT_CONTACT VARCHAR(32),
DPT_NOTES TEXT,
DPT_CONTACT2 VARCHAR(32),
DPT_EMAIL2 VARCHAR(80),
DPT_MOBILE VARCHAR(18),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DPT_ID)
);

CREATE TABLE DSCDET                                                                                       
(
DSD_ID INT NOT NULL,
DSH_ID INT NOT NULL,
DSD_LEVEL INT,
ROWVER INT DEFAULT 0,
DSD_UPPC DECIMAL,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DSD_ID)
);

CREATE TABLE DSCHDR                                                                                       
(
DSH_ID INT NOT NULL,
DSH_CODE VARCHAR(6),
DSH_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DSH_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DSH_ID)
);

CREATE TABLE DVRASCODE                                                                                    
(
DAC_ID INT NOT NULL,
DAC_CODE VARCHAR(6),
DAC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DAC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DAC_ID)
);

CREATE TABLE DVRASITEM                                                                                    
(
DSI_ID INT NOT NULL,
DAS_ID INT NOT NULL,
DIT_ID INT NOT NULL,
DSI_COMMENT VARCHAR(40),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DSI_ID)
);

CREATE TABLE DVRASSESS                                                                                    
(
DAS_ID INT NOT NULL,
DAC_ID INT NOT NULL,
DAS_SUBJECT VARCHAR(32),
DAS_PLANDT DATE,
DAS_ACTDT DATE,
DAS_NEXTDT DATE,
DAS_NOTES TEXT,
ROWVER INT DEFAULT 0,
STF_ID INT NOT NULL,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DAS_ID)
);

CREATE TABLE DVRFLEET                                                                                     
(
DFL_ID INT NOT NULL,
FLT_ID INT NOT NULL,
STF_ID INT NOT NULL,
USER_ID INT NOT NULL,
DFL_DTENT DATETIME(6),
DFL_DTST DATETIME(6),
DFL_DTEND DATETIME(6),
DFL_ODOST INT,
DFL_ODOEND INT,
DFL_COMM VARCHAR(128),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DFL_ID)
);

CREATE TABLE DVRITCODE                                                                                    
(
DIT_ID INT NOT NULL,
DIT_CODE VARCHAR(6),
DIT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DIT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DIT_ID)
);

CREATE TABLE DVRLCAT                                                                                      
(
DLC_ID INT NOT NULL,
DLC_CODE VARCHAR(6),
DLC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DLC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DLC_ID)
);

CREATE TABLE DVRLIC                                                                                       
(
DVL_ID INT NOT NULL,
STF_ID INT NOT NULL,
DLT_ID INT NOT NULL,
DVL_STARTDT DATE,
DVL_EXPDT DATE,
DVL_SUBJECT VARCHAR(32),
DVL_SERNO VARCHAR(20),
DVL_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DVL_ID)
);

CREATE TABLE DVRLITEM                                                                                     
(
DLI_ID INT NOT NULL,
DVL_ID INT NOT NULL,
DLC_ID INT NOT NULL,
DLI_COMMENT VARCHAR(32),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DLI_ID)
);

CREATE TABLE DVRLTYPE                                                                                     
(
DLT_ID INT NOT NULL,
DLT_CODE VARCHAR(6),
DLT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DLT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DLT_ID)
);

CREATE TABLE DVROFFCODE                                                                                   
(
DOC_ID INT NOT NULL,
DOC_CODE VARCHAR(6),
DOC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DOC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DOC_ID)
);

CREATE TABLE DVROFFENCE                                                                                   
(
DOF_ID INT NOT NULL,
STF_ID INT NOT NULL,
DOC_ID INT NOT NULL,
DOF_DATE DATE,
DOF_POINTS DECIMAL,
DOF_FINE DECIMAL,
DOF_CTDATE DATE,
DOF_SUBJECT VARCHAR(32),
DOF_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DOF_ID)
);

CREATE TABLE DVRTYP                                                                                       
(
DVT_ID INT NOT NULL,
DVT_CODE VARCHAR(6),
DVT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
DVT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DVT_ID)
);

CREATE TABLE ENGINEEURO                                                                                   
(
ENGE_ID INT NOT NULL,
ENGE_NAME VARCHAR(12) NOT NULL,
ENGE_RATE INT NOT NULL,
ROWVER INT DEFAULT 0,
ENGE_INACTIVE TINYINT(1) DEFAULT 0,
PRIMARY KEY (ENGE_ID)
);

CREATE TABLE EQPLOCHIST                                                                                   
(
EQLH_ID INT NOT NULL,
EQP_ID INT NOT NULL,
LOCID_OLD INT,
LOCID_NEW INT,
EQLH_DTIME DATETIME(6),
EQLH_COMMENT VARCHAR(40),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (EQLH_ID)
);

CREATE TABLE EQPSVCSCH                                                                                    
(
ESCH_ID INT NOT NULL,
EQP_ID INT NOT NULL,
ESVT_ID INT NOT NULL,
NOM_ID INT,
RPC_ID INT,
ESCH_TIMEPER INT,
ESCH_TIMEBASE TINYINT(1),
ESCH_ODOPER INT,
ESCH_DATENXT DATE,
ESCH_ODONXT INT,
ESCH_ESTHRS DECIMAL,
ESCH_SEQNO INT,
ESCH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ESCH_ID)
);

CREATE TABLE EQPSVCTYPE                                                                                   
(
ESVT_ID INT NOT NULL,
ESVT_CODE VARCHAR(6),
ESVT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
ESVT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
ESVT_NOTES TEXT,
PRIMARY KEY (ESVT_ID)
);

CREATE TABLE EQPT                                                                                         
(
EQP_ID INT NOT NULL,
SUP_ID INT,
COS_ID INT,
LOC_ID INT,
MAINT_ID INT,
EQP_ASSETNO VARCHAR(15),
EQP_DESC VARCHAR(25),
EQP_MNF VARCHAR(25),
EQP_MODEL VARCHAR(25),
EQP_SERNO VARCHAR(25),
EQP_DTPURCH DATE,
EQP_DTDISPO DATE,
EQP_NETT DECIMAL DEFAULT 0,
EQP_NOTES TEXT,
ROWVER INT DEFAULT 0,
EQT_ID INT,
EQP_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
EQP_HRSODO INT,
PRIMARY KEY (EQP_ID)
);

CREATE TABLE EQPTYPE                                                                                      
(
EQT_ID INT NOT NULL,
EQT_CODE VARCHAR(6),
EQT_NAME VARCHAR(32),
EQT_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (EQT_ID)
);

CREATE TABLE EXTMAINTD                                                                                    
(
EMD_ID INT NOT NULL,
EMH_ID INT NOT NULL,
FLT_ID INT NOT NULL,
RPA_ID INT NOT NULL,
RPC_ID INT NOT NULL,
RCC_ID INT NOT NULL,
EMD_WKDATE DATE,
EMD_DESC VARCHAR(65),
EMD_TYPE INT,
EMD_QTY DECIMAL DEFAULT 0,
EMD_RATE DECIMAL DEFAULT 0,
EMD_NETT DECIMAL DEFAULT 0,
EMD_WARREXP DATE,
ROWVER INT DEFAULT 0,
WKT_ID INT,
WKO_ID INT,
EMD_PARTS DECIMAL DEFAULT 0,
EMD_LAB DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (EMD_ID)
);

CREATE TABLE EXTMAINTH                                                                                    
(
EMH_ID INT NOT NULL,
SUP_ID INT NOT NULL,
EMH_SYSREF VARCHAR(10) NOT NULL,
EMH_EXTREF VARCHAR(15),
EMH_INVDATE DATE,
ROWVER INT DEFAULT 0,
EMH_ORDDATE DATE,
EMH_NOTES TEXT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (EMH_ID)
);

CREATE TABLE FLEET                                                                                        
(
FLT_ID INT NOT NULL,
FLT_INTNO VARCHAR(12),
FLT_REGNO VARCHAR(12),
MOT_ID INT,
MAINT_ID INT,
VCL_ID INT,
FTP_ID INT,
VTP_ID INT,
RDX_ID INT,
COS_ID INT,
BUD_ID INT,
NOM_ID INT,
CON_ID INT,
DPT_ID INT,
FTP_ID2 INT,
LOC_ID INT,
MDL_ID INT,
MNF_ID INT,
TYP_ID INT,
CUS_ID INT,
SUP_ID INT,
LDG_ID INT,
DATE_MANF DATE,
DATE_START DATE,
DATE_END DATE,
DATE_REGD DATE,
NONSERVICE TINYINT(1) DEFAULT 0,
ENG_MNF VARCHAR(30),
ENG_TYPE VARCHAR(20),
ENG_CC DECIMAL,
ENG_NO VARCHAR(20),
ENG_OIL VARCHAR(10),
ENG_NOTES TEXT,
TRANS_MNF VARCHAR(30),
TRANS_TYPE VARCHAR(20),
TRANS_NO VARCHAR(20),
TRANS_FLUID VARCHAR(10),
TRANS_NOTES TEXT,
ULWEIGHT DECIMAL,
GVWEIGHT DECIMAL,
GTWEIGHT DECIMAL,
PLTWEIGHT DECIMAL,
VLENGTH DECIMAL,
VWIDTH DECIMAL,
VHEIGHT DECIMAL,
ODO DECIMAL,
ODOUNIT CHAR(1),
IGN_KEY VARCHAR(10),
DOOR_KEY VARCHAR(10),
RADIOCODE VARCHAR(10),
LIFE_STATUS TINYINT(1),
ROWVER INT DEFAULT 0,
FLT_INACTIVE TINYINT(1) DEFAULT 0,
ODO_DTUPDATE DATETIME(6),
BODY_VIN VARCHAR(30),
BODY_MODELNO VARCHAR(20),
BODY_PAINTCODE VARCHAR(30),
TYRESIZEF VARCHAR(15),
TYRESIZEM VARCHAR(15),
TYRESIZER VARCHAR(15),
SEAT_CAP INT,
BODY_NOTES TEXT,
PURCHF_NAME VARCHAR(40),
PURCHF_ADD1 VARCHAR(30),
PURCHF_ADD2 VARCHAR(30),
PURCHF_ADD3 VARCHAR(30),
PURCHF_ADD4 VARCHAR(30),
PURCHF_PCODE VARCHAR(9),
PURCHF_TEL VARCHAR(18),
PURCHF_DATE DATETIME(6),
PURCH_BASE DECIMAL DEFAULT 0,
PURCH_BODY DECIMAL DEFAULT 0,
PURCH_BODYTEXT VARCHAR(35),
PURCH_EQPT DECIMAL DEFAULT 0,
PURCH_EQPTTEXT VARCHAR(35),
PURCH_OTHER DECIMAL DEFAULT 0,
PURCH_OTHERTEXT VARCHAR(35),
PURCH_TOTAL DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
BODYMANF VARCHAR(20),
CHASSISNO VARCHAR(20),
AXLES VARCHAR(10),
BODYTYPE VARCHAR(20),
EURORATING VARCHAR(10),
EMISSCAT VARCHAR(32),
VEHIMG TEXT,
ENGE_ID INT,
FLT_NOTES TEXT,
AXLEWT1 INT,
AXLEWT2 INT,
AXLEWT3 INT,
AXLEWT4 INT,
AXLEDWT1 INT,
AXLEDWT2 INT,
AXLEDWT3 INT,
AXLEDWT4 INT,
AXLETS1 VARCHAR(18),
AXLETS2 VARCHAR(18),
AXLETS3 VARCHAR(18),
AXLETS4 VARCHAR(18),
AXLEPLY1 VARCHAR(15),
AXLEPLY2 VARCHAR(15),
AXLEPLY3 VARCHAR(15),
AXLEPLY4 VARCHAR(15),
AXLESD1 CHAR(1),
AXLESD2 CHAR(1),
AXLESD3 CHAR(1),
AXLESD4 CHAR(1),
OLICTYPE VARCHAR(3),
PRIMARY KEY (FLT_ID)
);

CREATE TABLE FLEETCHG                                                                                     
(
FCG_ID INT NOT NULL,
FLT_ID INT NOT NULL,
CHG_ID INT NOT NULL,
FCG_TIMEPER INT,
FCG_TIMEBASE TINYINT(1),
FCG_DATENXT DATE,
FCG_DATELAST DATE,
ROWVER INT DEFAULT 0,
FCG_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
FCG_DATEFROM DATE,
PRIMARY KEY (FCG_ID)
);

CREATE TABLE FLEETEQPT                                                                                    
(
FLQ_ID INT NOT NULL,
FLT_ID INT NOT NULL,
EQP_ID INT,
FLQ_DTON DATETIME(6),
FLQ_DTOFF DATETIME(6),
FLQ_COMMENT VARCHAR(40),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FLQ_ID)
);

CREATE TABLE FLEETIMG                                                                                     
(
FIMG_ID INT NOT NULL,
FLT_ID INT NOT NULL,
CREATE_ID INT,
FIMG_DTFROM DATE,
FIMG_DTTO DATE,
FIMG_DTUPLOAD DATE,
FIMG_DESC VARCHAR(60),
FIMG_FILE VARCHAR(255),
FIMG_NOTES TEXT,
FIMG_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FIMG_ID)
);

CREATE TABLE FLEETLOCHIST                                                                                 
(
FLTLH_ID INT NOT NULL,
FLT_ID INT NOT NULL,
LOCID_OLD INT,
LOCID_NEW INT,
FLTLH_DTMOVE DATETIME(6),
FLTLH_DTENTRY DATETIME(6),
FLTLH_COMMENT VARCHAR(40),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FLTLH_ID)
);

CREATE TABLE FLEETSVC                                                                                     
(
FSC_ID INT NOT NULL,
FLT_ID INT NOT NULL,
SVC_ID INT NOT NULL,
CKL_ID INT NOT NULL,
FSC_TIMEPER INT,
FSC_TIMEBASE TINYINT(1),
FSC_ODOPER INT,
FSC_DATENXT DATE,
FSC_ODONXT INT,
FSC_HRS DECIMAL,
ROWVER INT DEFAULT 0,
FSC_INACTIVE TINYINT(1) DEFAULT 0,
FSC_SEQNO INT,
NOM_ID INT,
RPC_ID INT,
CRSYNC_ID INT NOT NULL,
FSC_ODOWARN INT,
PRIMARY KEY (FSC_ID)
);

CREATE TABLE FLEETTEMPL                                                                                   
(
FTM_ID INT NOT NULL,
FTM_NAME VARCHAR(35) NOT NULL,
MDL_ID INT,
MNF_ID INT,
VCL_ID INT,
VTP_ID INT,
SUP_ID INT,
COS_ID INT,
BUD_ID INT,
CON_ID INT,
DPT_ID INT,
NOM_ID INT,
CUS_ID INT,
LOC_ID INT,
LC2_ID INT,
MOT_ID INT,
RDX_ID INT,
FTP_ID INT,
FT2_ID INT,
ENG_MNF VARCHAR(30),
ENG_TYPE VARCHAR(20),
ENG_CC INT,
TRANS_MNF VARCHAR(30),
TRANS_TYPE VARCHAR(20),
BODYMDLNO VARCHAR(20),
ULWEIGHT INT,
GVWEIGHT INT,
GTWEIGHT INT,
PLTWEIGHT INT,
VLENGTH DECIMAL,
VWIDTH DECIMAL,
VHEIGHT DECIMAL,
FTM_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FTM_ID)
);

CREATE TABLE FRMLIST                                                                                      
(
FML_ID INT NOT NULL,
FML_FRMNAME VARCHAR(50),
FML_CAPTION CHAR(21),
FML_PAGENAME VARCHAR(30),
FML_GRPNAME VARCHAR(45),
FML_RESNAME VARCHAR(40),
FML_SMALLICON TINYINT(1) DEFAULT 0,
FML_NOTES VARCHAR(1024),
FML_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
MPN_ID INT,
MGR_ID INT,
FML_ORD INT,
PRIMARY KEY (FML_ID)
);

CREATE TABLE FUELBATIMP                                                                                   
(
FBT_ID INT NOT NULL,
FBT_DTIME DATETIME(6),
FBT_SYSREF VARCHAR(7),
FBT_DESC VARCHAR(65),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FBT_ID)
);

CREATE TABLE FUELCARD                                                                                     
(
FLC_ID INT NOT NULL,
CUS_ID INT NOT NULL,
FLC_CARDNO VARCHAR(16),
FLC_DTFROM DATETIME(6),
FLC_DTTO DATETIME(6),
ROWVER INT DEFAULT 0,
FLC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FLC_ID)
);

CREATE TABLE FUELIMPRAW                                                                                   
(
SRCTYPE INT NOT NULL,
RUNDATE DATETIME(6) NOT NULL,
DTTRANS DATETIME(6) NOT NULL,
QTY DECIMAL DEFAULT 0,
UCOST DECIMAL DEFAULT 0,
REGNO VARCHAR(8),
CARDNO VARCHAR(16),
ODO INT,
UPDREGNO VARCHAR(8),
UPDCARDNO VARCHAR(16),
EXREASON VARCHAR(80),
PROCESSOPT INT,
BATCHREF CHAR(7),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DTTRANS,RUNDATE,SRCTYPE)
);

CREATE TABLE FUELTRAN                                                                                     
(
FTN_ID INT NOT NULL,
FTP_ID INT NOT NULL,
CUS_ID INT NOT NULL,
VAT_ID INT NOT NULL,
FLT_ID INT,
FTN_METHOD VARCHAR(2),
FTN_DTENTRY DATETIME(6),
FTN_DTRANS DATETIME(6),
FTN_QTY DECIMAL DEFAULT 0,
FTN_UCOST DECIMAL DEFAULT 0,
FTN_NETT DECIMAL DEFAULT 0,
FTN_VCODE VARCHAR(1),
FTN_VATAMT DECIMAL DEFAULT 0,
FTN_GROSS DECIMAL DEFAULT 0,
FTN_ODO INT,
FTN_FLTNO VARCHAR(8),
FTN_REGNO VARCHAR(8),
FTN_CARDNO VARCHAR(16),
FTN_INV TINYINT(1) DEFAULT 0,
FTN_CUSCODE VARCHAR(6),
ROWVER INT DEFAULT 0,
FTN_INACTIVE TINYINT(1) DEFAULT 0,
FTN_VATINCLD TINYINT(1) DEFAULT 0,
FBT_ID INT,
FTN_MPG DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FTN_ID)
);

CREATE TABLE FUELTYPE                                                                                     
(
FTP_ID INT NOT NULL,
FTP_CODE VARCHAR(6),
FTP_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
FTP_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (FTP_ID)
);

CREATE TABLE GDDTL                                                                                        
(
GDD_ID INT NOT NULL,
GDH_ID INT NOT NULL,
POD_ID INT NOT NULL,
GDD_QTY DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
GDD_UCOST DECIMAL DEFAULT 0,
GDD_NETT DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (GDD_ID)
);

CREATE TABLE GDHDR                                                                                        
(
GDH_ID INT NOT NULL,
POH_ID INT NOT NULL,
STF_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
LOCK_ID INT,
GDH_SYSCODE VARCHAR(8),
GDH_REFCODE VARCHAR(15),
DATE_CREATE DATETIME(6),
GDH_DATE DATETIME(6),
GDH_STATUS INT,
GDH_NOTES VARCHAR(120),
GDH_NETT DECIMAL DEFAULT 0,
GDH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (GDH_ID)
);

CREATE TABLE GENSYS                                                                                       
(
GSY_ID VARCHAR(20) NOT NULL,
GSY_NEXTID INT,
GSY_PREFIX VARCHAR(3),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (GSY_ID)
);

CREATE TABLE IMGTEST                                                                                      
(
IMG_ID INT NOT NULL,
IMG_NAME VARCHAR(80),
IMG_DATA TEXT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (IMG_ID)
);

CREATE TABLE ITEMSLIST                                                                                    
(
GROUPNAME VARCHAR(25) NOT NULL,
ITEMNAME VARCHAR(50) NOT NULL,
PRIMARY KEY (ITEMNAME,GROUPNAME)
);

CREATE TABLE LOCATION                                                                                     
(
LOC_ID INT NOT NULL,
LOC_CODE VARCHAR(6),
LOC_NAME VARCHAR(32),
LOC_ADD1 VARCHAR(30),
LOC_ADD2 VARCHAR(30),
LOC_ADD3 VARCHAR(30),
LOC_ADD4 VARCHAR(30),
LOC_PCODE VARCHAR(9),
LOC_TEL VARCHAR(18),
LOC_FAX VARCHAR(18),
LOC_EMAIL VARCHAR(80),
LOC_CONTACT VARCHAR(32),
LOC_NOTES TEXT,
LOC_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
LOC_CONTACT2 VARCHAR(32),
LOC_EMAIL2 VARCHAR(80),
LOC_MOBILE VARCHAR(18),
LOC_VEHICLES TINYINT(1) DEFAULT 0,
LOC_MAINTC TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
TRAFFICAREA VARCHAR(30),
OLICNUM VARCHAR(30),
MAXVEH INT,
MAXTLR INT,
PRIMARY KEY (LOC_ID)
);

CREATE TABLE MANF                                                                                         
(
MNF_ID INT NOT NULL,
MNF_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
MNF_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (MNF_ID)
);

CREATE TABLE MNUGRPNAME                                                                                   
(
MGR_ID INT NOT NULL,
MPN_ID INT,
MGR_NAME VARCHAR(30),
MGR_ORD INT,
MGR_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (MGR_ID)
);

CREATE TABLE MNUPGNAME                                                                                    
(
MPN_ID INT NOT NULL,
MPN_NAME VARCHAR(30),
MPN_ORD INT,
MPN_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (MPN_ID)
);

CREATE TABLE MODEL                                                                                        
(
MDL_ID INT NOT NULL,
MDL_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
MDL_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (MDL_ID)
);

CREATE TABLE MOTTYPE                                                                                      
(
MOT_ID INT NOT NULL,
MOT_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
MOT_INACTIVE TINYINT(1) DEFAULT 0,
MOT_CODE VARCHAR(6),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (MOT_ID)
);

CREATE TABLE NOMINAL                                                                                      
(
NOM_ID INT NOT NULL,
NOM_CODE VARCHAR(15),
NOM_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
NOM_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (NOM_ID)
);

CREATE TABLE PART                                                                                         
(
PART_ID INT NOT NULL,
PTC_ID INT NOT NULL,
RPC_ID INT NOT NULL,
PART_CODE VARCHAR(16),
PART_NAME VARCHAR(35),
PART_NOTES TEXT,
PART_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
PART_STYPE TINYINT(1),
PART_PRICEBASE TINYINT(1),
CRSYNC_ID INT NOT NULL,
VAT_ID INT,
PRIMARY KEY (PART_ID)
);

CREATE TABLE PARTCAT                                                                                      
(
PTC_ID INT NOT NULL,
PTC_CODE VARCHAR(6),
PTC_NAME VARCHAR(32),
PTC_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PTC_ID)
);

CREATE TABLE PARTISSH                                                                                     
(
PTIH_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
STF_ID INT NOT NULL,
LOC_ID INT NOT NULL,
DLV_ID INT NOT NULL,
CUS_ID INT,
DPT_ID INT,
PTIH_SYSCODE CHAR(8),
PTIH_DATE DATE,
PTIH_ORDREF VARCHAR(18),
PTIH_NOTES TEXT,
PTIH_CONFIRM INT,
PTIH_STATUS INT,
ROWVER INT DEFAULT 0,
PTIH_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PTIH_ID)
);

CREATE TABLE PARTISSTK                                                                                    
(
PTIS_ID INT NOT NULL,
PTIH_ID INT NOT NULL,
RPC_ID INT NOT NULL,
NOM_ID INT NOT NULL,
VAT_ID INT NOT NULL,
PTLOC_ID INT NOT NULL,
PTIS_QTY DECIMAL DEFAULT 0,
PTIS_UCOST DECIMAL DEFAULT 0,
PTIS_NETT DECIMAL DEFAULT 0,
PTIS_VATRATE DECIMAL DEFAULT 0,
PTIS_VATAMT DECIMAL DEFAULT 0,
PTIS_LINETOT DECIMAL DEFAULT 0,
PTIS_STATUS INT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PTIS_ID)
);

CREATE TABLE PARTLISTD                                                                                    
(
PLD_ID INT NOT NULL,
PLH_ID INT NOT NULL,
PART_ID INT NOT NULL,
PLD_QTY DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PLD_ID)
);

CREATE TABLE PARTLISTH                                                                                    
(
PLH_ID INT NOT NULL,
MNF_ID INT,
MDL_ID INT,
PLH_CODE VARCHAR(10),
PLH_NAME VARCHAR(50),
PLH_NOTES TEXT,
PLH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PLH_ID)
);

CREATE TABLE PARTLOC                                                                                      
(
PTLOC_ID INT NOT NULL,
LOC_ID INT NOT NULL,
NOM_ID INT NOT NULL,
PART_ID INT NOT NULL,
ROWVER INT DEFAULT 0,
PTLOC_INACTIVE TINYINT(1) DEFAULT 0,
PTLOC_QOH DECIMAL DEFAULT 0,
PTLOC_QOO DECIMAL DEFAULT 0,
PTLOC_QAL DECIMAL DEFAULT 0,
PTLOC_UCOST DECIMAL DEFAULT 0,
PTLOC_QMIN DECIMAL DEFAULT 0,
PTLOC_QMAX DECIMAL DEFAULT 0,
PTLOC_LASTCHECK DATE,
PTLOC_BIN VARCHAR(10),
PTLOC_USE TINYINT(1),
PTLOC_RRP DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PTLOC_USERRP TINYINT(1) DEFAULT 0,
PRIMARY KEY (PTLOC_ID)
);

CREATE TABLE PARTMNF                                                                                      
(
PTMNF_ID INT NOT NULL,
MNF_ID INT NOT NULL,
PART_ID INT NOT NULL,
PTMNF_CODE VARCHAR(35),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PTMNF_ID)
);

CREATE TABLE PARTSUPP                                                                                     
(
PTSUP_ID INT NOT NULL,
SUP_ID INT NOT NULL,
PART_ID INT NOT NULL,
PTSUP_CODE VARCHAR(35),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PTSUP_ID)
);

CREATE TABLE PCDTL                                                                                        
(
PCD_ID INT NOT NULL,
PCH_ID INT NOT NULL,
POD_ID INT NOT NULL,
VAT_ID INT NOT NULL,
PCD_DESCRIPTION VARCHAR(50),
PCD_QTY DECIMAL DEFAULT 0,
PCD_UCOST DECIMAL DEFAULT 0,
PCD_NETT DECIMAL DEFAULT 0,
PCD_VATRATE DECIMAL DEFAULT 0,
PCD_VATAMT DECIMAL DEFAULT 0,
PCD_LINETOT DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
RPC_ID INT,
NOM_ID INT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PCD_ID)
);

CREATE TABLE PCHDR                                                                                        
(
PCH_ID INT NOT NULL,
POH_ID INT NOT NULL,
STF_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
APPROVE_ID INT,
PCH_SYSCODE VARCHAR(8),
PCH_REFCODE VARCHAR(15),
DATE_CREATE DATETIME(6),
PCH_DATE DATETIME(6),
PCH_PMTDUE DATE,
PCH_STATUS INT,
PCH_NETT DECIMAL DEFAULT 0,
PCH_NOTES TEXT,
PCH_VATAMT DECIMAL DEFAULT 0,
PCH_TOTAMT DECIMAL DEFAULT 0,
PCH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PCH_ID)
);

CREATE TABLE PIDTL                                                                                        
(
PID_ID INT NOT NULL,
PIH_ID INT NOT NULL,
POD_ID INT NOT NULL,
VAT_ID INT NOT NULL,
PID_STATUS INT,
PID_QTY DECIMAL DEFAULT 0,
PID_UCOST DECIMAL DEFAULT 0,
PID_NETT DECIMAL DEFAULT 0,
PID_VATAMT DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
PID_LINETOT DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PID_ID)
);

CREATE TABLE PIHDR                                                                                        
(
PIH_ID INT NOT NULL,
POH_ID INT NOT NULL,
STF_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
APPROVE_ID INT,
PIH_SYSCODE VARCHAR(8),
PIH_REFCODE VARCHAR(15),
DATE_CREATE DATETIME(6),
PIH_DATE DATETIME(6),
PIH_PMTDUE DATE,
PIH_STATUS INT,
PIH_NOTES VARCHAR(120),
PIH_NETT DECIMAL DEFAULT 0,
PIH_VATAMT DECIMAL DEFAULT 0,
PIH_TOTAMT DECIMAL DEFAULT 0,
PIH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (PIH_ID)
);

CREATE TABLE PODTL                                                                                        
(
POH_ID INT NOT NULL,
POD_ID INT NOT NULL,
VAT_ID INT NOT NULL,
RPC_ID INT,
DPT_ID INT,
FLT_ID INT,
WKT_ID INT,
WKO_ID INT,
NOM_ID INT NOT NULL,
STK_ID INT,
PTLOC_ID INT,
POD_PARTNO VARCHAR(16),
POD_PARTDESC VARCHAR(30),
POD_QTY DECIMAL DEFAULT 0,
POD_UCOST DECIMAL DEFAULT 0,
POD_NETT DECIMAL DEFAULT 0,
POD_TYPE INT,
POD_SCHG TINYINT(1) DEFAULT 0,
POD_NOTE TEXT,
POD_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
POD_RRP DECIMAL DEFAULT 0,
PTIH_ID INT,
CUS_ID INT,
POD_DTDELIVBY DATETIME(6),
CRSYNC_ID INT NOT NULL,
POD_SUPINVNO CHAR(15),
POD_SUPDLVNO CHAR(15),
PRIMARY KEY (POD_ID)
);

CREATE TABLE POHDR                                                                                        
(
POH_ID INT NOT NULL,
SUP_ID INT NOT NULL,
LOC_ID INT NOT NULL,
STF_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
LOCK_ID INT,
POH_SYSCODE VARCHAR(8),
POH_REFCODE VARCHAR(15),
DATE_CREATE DATETIME(6),
DATE_CHECK DATETIME(6),
DATE_SENT DATETIME(6),
DATE_DUE DATETIME(6),
STK_NETT DECIMAL DEFAULT 0,
NSTK_NETT DECIMAL DEFAULT 0,
ORDER_NETT DECIMAL DEFAULT 0,
ORDER_VAT DECIMAL DEFAULT 0,
DISC_PC DECIMAL,
DISC_NETT DECIMAL DEFAULT 0,
DISC_VAT DECIMAL DEFAULT 0,
ORDER_TOTAL DECIMAL DEFAULT 0,
ORD_STATUS INT,
DELIV_STATUS INT,
INV_STATUS INT,
NOTES_INT TEXT,
NOTES_EXT TEXT,
POH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
SYS_CRDATE DATETIME(6),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (POH_ID)
);

CREATE TABLE REPACT                                                                                       
(
RPA_ID INT NOT NULL,
RPA_CODE VARCHAR(15),
RPA_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (RPA_ID)
);

CREATE TABLE REPCAT                                                                                       
(
RPC_ID INT NOT NULL,
RPC_CODE VARCHAR(18),
RPC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
RPC_INACTIVE TINYINT(1) DEFAULT 0,
RPC_CONTRACT TINYINT(1) DEFAULT 0,
RPC_TASKDESC VARCHAR(70),
NOM_ID INT,
CRSYNC_ID INT NOT NULL,
VAT_ID INT,
PRIMARY KEY (RPC_ID)
);

CREATE TABLE REPCHARGE                                                                                    
(
RCC_ID INT NOT NULL,
RCC_CODE VARCHAR(20),
RCC_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (RCC_ID)
);

CREATE TABLE REPITEM                                                                                      
(
RPI_ID INT NOT NULL,
RPC_ID INT NOT NULL,
RPI_DESC VARCHAR(65),
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (RPI_ID)
);

CREATE TABLE REPORTCATALOG                                                                                
(
RCT_ID INT NOT NULL,
RCT_TITLE VARCHAR(60),
RCT_NOTES TEXT,
RCT_CAT CHAR(10),
RCT_COMP CHAR(6),
RCT_LIB VARCHAR(30),
RCT_NSP VARCHAR(30),
RCT_CLS VARCHAR(40),
ROWVER INT DEFAULT 0,
RCT_REPNAME VARCHAR(35),
RCT_SUBCAT VARCHAR(20),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (RCT_ID)
);

CREATE TABLE ROADTAX                                                                                      
(
RDX_ID INT NOT NULL,
RDX_CODE VARCHAR(6),
RDX_NAME VARCHAR(32),
RDX_ANNCOST DECIMAL,
ROWVER INT DEFAULT 0,
RDX_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (RDX_ID)
);

CREATE TABLE ROWGEN                                                                                       
(
ROWNAME VARCHAR(25) NOT NULL,
ROWVALUE INT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (ROWNAME)
);

CREATE TABLE STAFF                                                                                        
(
STF_ID INT NOT NULL,
STF_DVR TINYINT(1) DEFAULT 0,
STF_TECH TINYINT(1) DEFAULT 0,
STF_CODE VARCHAR(6),
STF_SAL VARCHAR(4),
STF_FNAME VARCHAR(20),
STF_INIT VARCHAR(1),
STF_LNAME VARCHAR(20),
STF_ADD1 VARCHAR(32),
STF_ADD2 VARCHAR(32),
STF_ADD3 VARCHAR(32),
STF_ADD4 VARCHAR(32),
STF_PCODE VARCHAR(9),
STF_TEL VARCHAR(18),
STF_FAX VARCHAR(18),
STF_MOBILE VARCHAR(18),
STF_EMAIL VARCHAR(80),
STF_NOKNAME VARCHAR(35),
STF_NOKADD1 VARCHAR(32),
STF_NOKADD2 VARCHAR(32),
STF_NOKADD3 VARCHAR(32),
STF_NOKADD4 VARCHAR(32),
STF_NOKPCODE VARCHAR(9),
STF_NOKTEL VARCHAR(18),
STF_NOK2TEL VARCHAR(18),
STF_DOB DATE,
STF_DTSTART DATE,
STF_DTEND DATE,
STF_NOTES TEXT,
STF_INACTIVE INT DEFAULT 0,
ROWVER INT DEFAULT 0,
LOC_ID INT NOT NULL,
STF_NOK2NAME VARCHAR(32),
STF_NOKMOB VARCHAR(18),
CRSYNC_ID INT NOT NULL,
STF_PIN CHAR(4),
PRIMARY KEY (STF_ID)
);

CREATE TABLE STKCAT                                                                                       
(
STC_ID INT NOT NULL,
STC_CODE VARCHAR(6),
STC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
STC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (STC_ID)
);

CREATE TABLE STKPARTNO                                                                                    
(
STP_ID INT NOT NULL,
STK_ID INT NOT NULL,
SUP_ID INT NOT NULL,
STP_PARTNO VARCHAR(15),
ROWVER INT DEFAULT 0,
STP_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (STP_ID)
);

CREATE TABLE STOCK                                                                                        
(
STK_ID INT NOT NULL,
STK_CODE VARCHAR(16),
STK_NAME VARCHAR(32),
STK_NOTES TEXT,
STK_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
STK_QTY DECIMAL DEFAULT 0,
STK_UCOST DECIMAL DEFAULT 0,
STK_ONORD DECIMAL DEFAULT 0,
STK_ONJOB DECIMAL DEFAULT 0,
STK_MIN DECIMAL DEFAULT 0,
STK_MAX DECIMAL DEFAULT 0,
STK_LASTCHECK DATE,
STC_ID INT NOT NULL,
RPC_ID INT NOT NULL,
STK_LASTQTY DECIMAL,
STK_IMPREST TINYINT(1) DEFAULT 0,
STK_DISC TINYINT(1),
WKS_ID INT NOT NULL,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (STK_ID)
);

CREATE TABLE SUPPLIER                                                                                     
(
SUP_ID INT NOT NULL,
SUP_CODE VARCHAR(6),
SUP_NAME VARCHAR(32),
SUP_ADD1 VARCHAR(30),
SUP_ADD2 VARCHAR(30),
SUP_ADD3 VARCHAR(30),
SUP_ADD4 VARCHAR(30),
SUP_PCODE VARCHAR(9),
SUP_TEL VARCHAR(18),
SUP_FAX VARCHAR(18),
SUP_EMAIL VARCHAR(80),
SUP_CONTACT VARCHAR(32),
SUP_NOTES TEXT,
SUP_INACTIVE INT DEFAULT 0,
ROWVER INT DEFAULT 0,
SUP_CONTACT2 VARCHAR(32),
SUP_EMAIL2 VARCHAR(80),
SUP_MOBILE VARCHAR(18),
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (SUP_ID)
);

CREATE TABLE SVCTYPE                                                                                      
(
SVC_ID INT NOT NULL,
SVC_CODE VARCHAR(6),
SVC_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
SVC_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
SVC_NOTES TEXT,
PRIMARY KEY (SVC_ID)
);

CREATE TABLE SYSPREF                                                                                      
(
SPF_ID INT NOT NULL,
SPF_NAME VARCHAR(30),
SPF_TYPE CHAR(1),
SPF_VALUE VARCHAR(30),
SPF_DESC VARCHAR(50),
SPF_SYSFLAG TINYINT(1) DEFAULT 0,
SPF_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (SPF_ID)
);

CREATE TABLE TECHEMPHRS                                                                                   
(
TCHE_ID INT NOT NULL,
STF_ID INT NOT NULL,
DT_START DATETIME(6),
DT_END DATETIME(6),
TOT_HRS DECIMAL DEFAULT 0,
BREAK_HRS DECIMAL DEFAULT 0,
STD_HRS DECIMAL DEFAULT 0,
OT_HRS DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
TCHE_INACTIVE TINYINT(1) DEFAULT 0,
PRIMARY KEY (TCHE_ID)
);

CREATE TABLE TECHJOBHRS                                                                                   
(
TJBH_ID INT NOT NULL,
STF_ID INT NOT NULL,
WKO_ID INT,
WKT_ID INT,
TJBH_DTENTRY DATETIME(6),
TJBH_TYPE INT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
TSK_STATUS TINYINT(1) NOT NULL,
PRIMARY KEY (TJBH_ID)
);

CREATE TABLE TMPEXTINV                                                                                    
(
TMPEXI_ID INT NOT NULL,
TMPEXI_INVNO VARCHAR(15),
SUP_ID INT,
TMPEXI_FNAME VARCHAR(255),
TMPEXI_STATUS INT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (TMPEXI_ID)
);

CREATE TABLE TMPEXTMD                                                                                     
(
TMPMD_ID INT NOT NULL,
TMPMH_ID INT NOT NULL,
VAT_ID INT,
ITM_DESC VARCHAR(65),
ITM_TYPE INT,
ITM_QTY DECIMAL DEFAULT 0,
ITM_RATE DECIMAL DEFAULT 0,
ITM_NETT DECIMAL DEFAULT 0,
ITM_VATRATE DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (TMPMD_ID)
);

CREATE TABLE TMPEXTMH                                                                                     
(
TMPMH_ID INT NOT NULL,
SUP_ID INT NOT NULL,
FLT_ID INT NOT NULL,
LOC_ID INT NOT NULL,
CUS_ID INT NOT NULL,
SUP_INVNO VARCHAR(15),
SUP_INVDATE DATE,
SUP_JOBREF VARCHAR(12),
JOBDESC VARCHAR(70),
JOB_NOTES TEXT,
TMPMH_PROC TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (TMPMH_ID)
);

CREATE TABLE TRAINCENT                                                                                    
(
DTC_ID INT NOT NULL,
DTC_CODE VARCHAR(6),
DTC_NAME VARCHAR(32),
DTC_ADD1 VARCHAR(32),
DTC_ADD2 VARCHAR(32),
DTC_ADD3 VARCHAR(32),
DTC_ADD4 VARCHAR(32),
DTC_PCODE VARCHAR(9),
DTC_TEL VARCHAR(18),
DTC_EMAIL VARCHAR(80),
DTC_CONTACT1 VARCHAR(32),
DTC_MOBILE1 VARCHAR(18),
DTC_CONTACT2 VARCHAR(32),
DTC_MOBILE2 VARCHAR(18),
DTC_NOTES TEXT,
DTC_INACTIVE INT DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DTC_ID)
);

CREATE TABLE TRAINREQ                                                                                     
(
DTR_ID INT NOT NULL,
DTR_CODE VARCHAR(6),
DTR_NAME VARCHAR(32),
DTR_NOTES TEXT,
DTR_INACTIVE INT DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (DTR_ID)
);

CREATE TABLE USERS                                                                                        
(
USER_ID INT NOT NULL,
USER_FLAGS INT,
USER_LOGIN VARCHAR(12) NOT NULL,
USER_PASS VARCHAR(255) NOT NULL,
USER_KEY VARCHAR(12) NOT NULL,
USER_FNAME VARCHAR(32),
USER_LNAME VARCHAR(30),
USER_DTLAST DATETIME(6),
STF_INACTIVE INT DEFAULT 0,
ROWVER INT DEFAULT 0,
USER_DTLOGON DATETIME(6),
USER_DTLOGOFF DATETIME(6),
CRSYNC_ID INT NOT NULL,
LOC_ID INT,
USER_LEVEL INT,
USER_SECLEVEL INT,
PRIMARY KEY (USER_ID)
);

CREATE TABLE USRGRPD                                                                                      
(
UGD_ID INT NOT NULL,
UGH_ID INT NOT NULL,
FML_ID INT NOT NULL,
UGD_ACCESS INT,
UGD_CODE INT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (UGD_ID)
);

CREATE TABLE USRGRPH                                                                                      
(
UGH_ID INT NOT NULL,
UGH_NAME VARCHAR(255),
UGH_CODE INT,
UGH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (UGH_ID)
);

CREATE TABLE USRMNUGRP                                                                                    
(
UMG_ID INT NOT NULL,
UGH_ID INT NOT NULL,
USER_ID INT NOT NULL,
UMG_CODE INT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (UMG_ID)
);

CREATE TABLE VATCODE                                                                                      
(
VAT_ID INT NOT NULL,
VAT_CODE VARCHAR(1),
VAT_NAME VARCHAR(32),
VAT_PCENT NUMERIC(6, 2),
VAT_EXPORT1 VARCHAR(2),
VAT_EXPORT2 VARCHAR(2),
ROWVER INT DEFAULT 0,
VAT_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (VAT_ID)
);

CREATE TABLE VEHCLASS                                                                                     
(
VCL_ID INT NOT NULL,
VCL_CODE VARCHAR(6),
VCL_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
VCL_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (VCL_ID)
);

CREATE TABLE VEHCONTRACT                                                                                  
(
VCN_ID INT NOT NULL,
FLT_ID INT NOT NULL,
CON_ID INT NOT NULL,
VCN_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (VCN_ID)
);

CREATE TABLE VEHTYPE                                                                                      
(
VTP_ID INT NOT NULL,
VTP_CODE VARCHAR(6),
VTP_NAME VARCHAR(32),
ROWVER INT DEFAULT 0,
VTP_INACTIVE TINYINT(1) DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (VTP_ID)
);

CREATE TABLE WKESTDTL                                                                                     
(
WED_ID INT NOT NULL,
WEH_ID INT NOT NULL,
RPC_ID INT NOT NULL,
STF_ID INT NOT NULL,
WED_DESC VARCHAR(45),
WED_ESTHRS DECIMAL DEFAULT 0,
WED_LABCOST DECIMAL DEFAULT 0,
WED_PARTSCOST DECIMAL DEFAULT 0,
WED_ACCEPT TINYINT(1) DEFAULT 0,
WED_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (WED_ID)
);

CREATE TABLE WKESTHDR                                                                                     
(
WEH_ID INT NOT NULL,
CUS_ID INT NOT NULL,
STF_ID INT NOT NULL,
USER_ID INT NOT NULL,
WKP_ID INT,
WKO_ID INT,
LOC_ID INT,
WEH_SYSNO VARCHAR(10),
WEH_REGNO VARCHAR(12),
MNF_ID INT NOT NULL,
MDL_ID INT NOT NULL,
WEH_DTMANF DATETIME(6),
WEH_DTEST DATETIME(6),
WEH_DTRQD DATETIME(6),
WEH_ORDNO VARCHAR(12),
WEH_DTORD DATETIME(6),
WEH_DESC VARCHAR(80),
WEH_NOTES TEXT,
ROWVER INT DEFAULT 0,
STF_IDEST INT,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (WEH_ID)
);

CREATE TABLE WKEXTD                                                                                       
(
WXD_ID INT NOT NULL,
WXH_ID INT NOT NULL,
WKO_ID INT NOT NULL,
WKT_ID INT NOT NULL,
VAT_ID INT NOT NULL,
WXD_TYPE INT,
WXD_DESC VARCHAR(120),
WXD_ESTNETT DECIMAL DEFAULT 0,
WXD_QTY DECIMAL DEFAULT 0,
WXD_UCOST DECIMAL DEFAULT 0,
WXD_ACTNETT DECIMAL DEFAULT 0,
WXD_VATRATE DECIMAL DEFAULT 0,
WXD_VATAMT DECIMAL DEFAULT 0,
WXD_TOTAL DECIMAL DEFAULT 0,
WXD_STATUS INT,
WXD_NOTES TEXT,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
NOM_ID INT,
WXD_INVUCOST DECIMAL DEFAULT 0,
NOM_ID_INV INT,
PRIMARY KEY (WXD_ID)
);

CREATE TABLE WKEXTH                                                                                       
(
WXH_ID INT NOT NULL,
SUP_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
WXH_SYSREF VARCHAR(8),
WXH_SUPREF VARCHAR(15),
WXH_DATE DATE,
WXH_DTQTE DATE,
WXH_DTSTART DATE,
WXH_DTFINISH DATE,
WXH_STATUS INT,
WXH_ESTNETT DECIMAL DEFAULT 0,
WXH_ACTNETT DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
WXH_INACTIVE TINYINT(1) DEFAULT 0,
WXH_VAT DECIMAL DEFAULT 0,
WXH_TOTAL DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
WXH_DTORD DATE,
WXH_POREF VARCHAR(15),
PRIMARY KEY (WXH_ID)
);

CREATE TABLE WKICREDD                                                                                     
(
WCD_ID INT NOT NULL,
WCH_ID INT NOT NULL,
WKD_ID INT,
RPC_ID INT NOT NULL,
VAT_ID INT NOT NULL,
WCD_TEXT VARCHAR(50),
WCD_QTY DECIMAL DEFAULT 0,
WCD_UCOST DECIMAL DEFAULT 0,
WCD_NETT DECIMAL DEFAULT 0,
WCD_VATRATE DECIMAL DEFAULT 0,
WCD_VATAMT DECIMAL DEFAULT 0,
WCD_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
NOM_ID INT NOT NULL,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (WCD_ID)
);

CREATE TABLE WKICREDH                                                                                     
(
WCH_ID INT NOT NULL,
WKI_ID INT NOT NULL,
STF_ID INT NOT NULL,
CREATE_ID INT NOT NULL,
APPROVE_ID INT,
WCH_SYSCODE VARCHAR(8),
WCH_STATUS INT,
DATE_CREATE DATETIME(6),
WCH_DATE DATETIME(6),
WCH_PMTDUE DATE,
WCH_NOTES VARCHAR(120),
WCH_NETT DECIMAL DEFAULT 0,
WCH_VATAMT DECIMAL DEFAULT 0,
WCH_TOTAMT DECIMAL DEFAULT 0,
WCH_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
WCH_EXPREF VARCHAR(12),
WCH_APPROVED DATETIME(6),
PRIMARY KEY (WCH_ID)
);

CREATE TABLE WKINVD                                                                                       
(
WKD_ID INT NOT NULL,
WKI_ID INT NOT NULL,
WKT_ID INT NOT NULL,
RPC_ID INT NOT NULL,
VAT_ID INT NOT NULL,
WTS_ID INT,
WTL_ID INT,
POD_ID INT,
WKD_TYPE INT NOT NULL,
WKD_TEXT VARCHAR(120),
WKD_QTY DECIMAL DEFAULT 0,
WKD_UCOST DECIMAL DEFAULT 0,
WKD_NETT DECIMAL DEFAULT 0,
WKD_VATRATE DECIMAL DEFAULT 0,
WKD_VATAMT DECIMAL DEFAULT 0,
WKD_LINETOT DECIMAL DEFAULT 0,
WKD_STATUS INT,
WKD_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
NOM_ID INT,
WXD_ID INT,
PRIMARY KEY (WKD_ID)
);

CREATE TABLE WKINVEXPORT                                                                                  
(
WIX_ID INT NOT NULL,
WIX_DTIME DATETIME(6),
WIX_SYSCODE VARCHAR(8),
WIX_NUMINV INT,
WIX_NETT DECIMAL DEFAULT 0,
WIX_VAT DECIMAL DEFAULT 0,
WIX_TOTAL DECIMAL DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (WIX_ID)
);

CREATE TABLE WKINVH                                                                                       
(
WKI_ID INT NOT NULL,
WKO_ID INT NOT NULL,
CUS_ID INT NOT NULL,
STF_ID INT NOT NULL,
WKI_DESCRIPTION VARCHAR(70),
WKI_NOTES TEXT,
WKI_INVDATE DATE,
WKI_PMTDUE DATE,
WKI_CREATED DATETIME(6),
WKI_APPROVED DATETIME(6),
WKI_STATUS INT,
WKI_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
WKI_SYSCODE CHAR(8),
WKI_INTNOTES TEXT,
WKI_CUSINVREF VARCHAR(15),
CRSYNC_ID INT NOT NULL,
WKI_EXPREF VARCHAR(12),
PRIMARY KEY (WKI_ID)
);

CREATE TABLE WKORDER                                                                                      
(
WKO_ID INT NOT NULL,
CUS_ID INT NOT NULL,
LOC_ID INT NOT NULL,
FLT_ID INT,
STF_ID INT,
WKO_SYSNUM VARCHAR(10),
WKO_REGNO VARCHAR(12) NOT NULL,
WKO_DESC VARCHAR(70),
WKO_MANF VARCHAR(32),
WKO_MODEL VARCHAR(32),
WKO_VEHMONTH VARCHAR(2),
WKO_VEHYEAR VARCHAR(4),
WKO_DATE DATE,
WKO_CUSORDNO VARCHAR(12),
WKO_DATEIN DATETIME(6),
WKO_DATEOUT DATETIME(6),
WKO_DATEDUE DATETIME(6),
WKO_DATECANCEL DATETIME(6),
WKO_CONFIRM TINYINT(1) DEFAULT 0,
WKO_DATECONF DATETIME(6),
WKO_NOTES1 TEXT,
WKO_NOTES2 TEXT,
WKO_ESTHRS DECIMAL,
WKO_ESTCOST DECIMAL,
WKO_CRITICAL TINYINT(1) DEFAULT 0,
WKO_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
WKO_REASON TINYINT(1),
WKO_ODO INT,
CRSYNC_ID INT NOT NULL,
WKO_STATUS INT,
WKO_BILLNAME VARCHAR(32),
WKO_BILLADDR1 VARCHAR(32),
WKO_BILLADDR2 VARCHAR(32),
WKO_BILLADDR3 VARCHAR(32),
WKO_BILLADDR4 VARCHAR(32),
WKO_BILLPCODE VARCHAR(9),
WKO_BILLTEL VARCHAR(18),
WKO_BILLMOBILE VARCHAR(18),
WKO_BILLCONTACT VARCHAR(32),
WKO_BILLEMAIL VARCHAR(80),
WKO_BILLFLAG TINYINT(1) DEFAULT 0,
PRIMARY KEY (WKO_ID)
);

CREATE TABLE WKPLAN                                                                                       
(
WKP_ID INT NOT NULL,
WKO_ID INT,
LOC_ID INT,
FSC_ID INT,
FSC_SEQNO INT,
WEH_ID INT,
DFT_ID INT,
FLT_ID INT,
WKP_SDTIME DATETIME(6),
WKP_ODTIME DATETIME(6),
WKP_EDTIME DATETIME(6),
WKP_DESC VARCHAR(40),
WKP_NOTES TEXT,
ROWVER INT DEFAULT 0,
WKP_TYPE INT,
WKP_DATE DATE,
CRSYNC_ID INT NOT NULL,
PRIMARY KEY (WKP_ID)
);

CREATE TABLE WKTASK                                                                                       
(
WKT_ID INT NOT NULL,
WKO_ID INT NOT NULL,
RPC_ID INT NOT NULL,
NOM_ID INT NOT NULL,
FSC_ID INT,
DFT_ID INT,
WEH_ID INT,
WKT_TYPE VARCHAR(1),
WKT_DESC VARCHAR(70),
WKT_ESTHRS DECIMAL,
WKT_ACTHRS DECIMAL,
WKT_NOTES TEXT,
WKT_STATUS INT,
ROWVER INT DEFAULT 0,
WKT_ESTPARTS DECIMAL DEFAULT 0,
WKT_ACTPARTS DECIMAL DEFAULT 0,
CRSYNC_ID INT NOT NULL,
WKT_FIXCOST TINYINT(1) DEFAULT 0,
WKT_NETT DECIMAL DEFAULT 0,
VAT_ID INT,
PRIMARY KEY (WKT_ID)
);

CREATE TABLE WKTLAB                                                                                       
(
WTL_ID INT NOT NULL,
WKT_ID INT NOT NULL,
WKO_ID INT NOT NULL,
STF_ID INT NOT NULL,
WTL_DTSTART DATETIME(6),
WTL_DTEND DATETIME(6),
WTL_HRS DECIMAL DEFAULT 0,
WTL_RATE DECIMAL DEFAULT 0,
WTL_NETT DECIMAL DEFAULT 0,
WTL_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
CRSYNC_ID INT NOT NULL,
WTL_DESC VARCHAR(120),
PRIMARY KEY (WTL_ID)
);

CREATE TABLE WKTSTOCK                                                                                     
(
WTS_ID INT NOT NULL,
WKT_ID INT NOT NULL,
WKO_ID INT NOT NULL,
PTLOC_ID INT NOT NULL,
WTS_QTY DECIMAL DEFAULT 0,
WTS_UCOST DECIMAL DEFAULT 0,
WTS_NETT DECIMAL DEFAULT 0,
WTS_INACTIVE TINYINT(1) DEFAULT 0,
ROWVER INT DEFAULT 0,
RPC_ID INT,
NOM_ID INT,
CRSYNC_ID INT NOT NULL,
VAT_ID INT,
PRIMARY KEY (WTS_ID)
);

